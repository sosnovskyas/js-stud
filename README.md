#ДЗ

## Slider
###алгоритм работы
```
конструктор(elem, minValue, maxValue, currentValue)
                  |
                  V
- добавляем в родительский элемент всё необходимое (line, thumb)
- возвращаем объект слайдера _slider создаваемый методом makeSlider
- считаем технические переменные (_point)
- настраиваем стартовые параметры
                  |
                  V

```